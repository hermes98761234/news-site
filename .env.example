# Server
SERVER_PORT=3000
DATABASE_URL=/data/news.db
STATIC_DIR=/app/static
RUST_LOG=info

# Cache
VALKEY_URL=redis://valkey:6379

# Auth — CHANGE THIS in production
CLI_TOKEN=change-me-in-production

# Frontend build arg (set at build time)
NEXT_PUBLIC_API_URL=/
